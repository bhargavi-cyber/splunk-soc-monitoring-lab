# Host Activity Monitoring
# Purpose:
# This query counts the number of events generated by each host.

index=main sourcetype="eventgen"
| stats count by host
| sort -count

# Outcome:
# Displays the number of events generated by each host system.
# Helps identify systems producing abnormal activity levels.
